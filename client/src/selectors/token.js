export const getToken = (state) => state.token;
export const getIdToken = (state) => state.token.id;
export const getUsername = (state) => state.token.username;
export const getTokenToken = (state) => state.token.token;
